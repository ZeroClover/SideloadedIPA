diff --git a/src/bundle.cpp b/src/bundle.cpp
index 1ce62ce..49b36ec 100644
--- a/src/bundle.cpp
+++ b/src/bundle.cpp
@@ -318 +318,5 @@ bool ZBundle::SignNode(jvalue& jvNode)
-				if (!macho.Sign(m_pSignAsset, m_bForceSign, "", "", "", "")) {
+				string strEntitleData = m_pSignAsset->m_strEntitleData;
+				m_pSignAsset->m_strEntitleData.clear();
+				bool bSigned = macho.Sign(m_pSignAsset, m_bForceSign, "", "", "", "");
+				m_pSignAsset->m_strEntitleData = strEntitleData;
+				if (!bSigned) {
@@ -403,0 +408,17 @@ bool ZBundle::SignNode(jvalue& jvNode)
+	if (m_pSignAssets) {
+		auto endsWith = [](const string& str, const string& suffix) {
+			return str.size() >= suffix.size() && 0 == str.compare(str.size()-suffix.size(), suffix.size(), suffix);
+		};
+
+		for (auto it = m_pSignAssets->rbegin(); it != m_pSignAssets->rend(); ++it) {
+			m_pSignAsset = &(*it);
+			if (endsWith(m_pSignAsset->m_strApplicationId, strBundleId)) {
+				if (!ZFile::WriteFileV(m_pSignAsset->m_strProvData, "%s/%s/embedded.mobileprovision", m_strAppFolder.c_str(), strFolder.c_str())) {
+					ZLog::ErrorV(">>> Can't write embedded.mobileprovision!\n");
+					return false;
+				}
+				break;
+			}
+		}
+	}
+
@@ -449,34 +469,0 @@ bool ZBundle::SignNode(jvalue& jvNode)
-	if (m_pSignAssets) {
-		auto endsWith = [](const string& str, const string& suffix) {
-			return str.size() >= suffix.size() && 0 == str.compare(str.size()-suffix.size(), suffix.size(), suffix);
-		};
-
-		for (auto it = m_pSignAssets->rbegin(); it != m_pSignAssets->rend(); ++it) {
-			m_pSignAsset = &(*it);
-			if (endsWith(m_pSignAsset->m_strApplicationId, strBundleId)) {
-				if (!ZFile::WriteFileV(m_pSignAsset->m_strProvData, "%s/%s/embedded.mobileprovision", m_strAppFolder.c_str(), strFolder.c_str())) {
-					ZLog::ErrorV(">>> Can't write embedded.mobileprovision!\n");
-					return false;
-				}
-				break;
-			}
-		}
-	}
-
-	if (m_pSignAssets) {
-		auto endsWith = [](const string& str, const string& suffix) {
-			return str.size() >= suffix.size() && 0 == str.compare(str.size()-suffix.size(), suffix.size(), suffix);
-		};
-
-		for (auto it = m_pSignAssets->rbegin(); it != m_pSignAssets->rend(); ++it) {
-			m_pSignAsset = &(*it);
-			if (endsWith(m_pSignAsset->m_strApplicationId, strBundleId)) {
-				if (!ZFile::WriteFileV(m_pSignAsset->m_strProvData, "%s/%s/embedded.mobileprovision", m_strAppFolder.c_str(), strFolder.c_str())) {
-					ZLog::ErrorV(">>> Can't write embedded.mobileprovision!\n");
-					return false;
-				}
-				break;
-			}
-		}
-	}
-
diff --git a/src/zsign.cpp b/src/zsign.cpp
index a5ce7e6..ababe36 100644
--- a/src/zsign.cpp
+++ b/src/zsign.cpp
@@ -136 +136 @@ int usage()
-	ZLog::Print("-e, --entitlements\tNew entitlements to change.\n");
+	ZLog::Print("-e, --entitlements\tNew entitlements to change. Repeat to pair with repeated -m profiles.\n");
@@ -191,0 +192 @@ int main(int argc, char* argv[])
+	vector<string> arrEntitleFiles;
@@ -235,0 +237 @@ int main(int argc, char* argv[])
+			arrEntitleFiles.push_back(strEntitleFile);
@@ -330,0 +333,4 @@ int main(int argc, char* argv[])
+	if (arrEntitleFiles.size() > 1 && arrEntitleFiles.size() != arrProvFiles.size()) {
+		ZLog::ErrorV(">>> Repeated entitlements must match the number and order of provisioning profiles.\n");
+		return -1;
+	}
@@ -460 +466,3 @@ int main(int argc, char* argv[])
-		for (const string& provFile : arrProvFiles) {
+		for (size_t i = 0; i < arrProvFiles.size(); i++) {
+			const string& provFile = arrProvFiles[i];
+			const string& entitleFile = arrEntitleFiles.size() > 1 ? arrEntitleFiles[i] : strEntitleFile;
@@ -462 +470 @@ int main(int argc, char* argv[])
-			if (!zsaList.back().Init(strCertFile, strPKeyFile, provFile, strEntitleFile, strPassword, bAdhoc, bSHA256Only, false)) {
+			if (!zsaList.back().Init(strCertFile, strPKeyFile, provFile, entitleFile, strPassword, bAdhoc, bSHA256Only, false)) {
