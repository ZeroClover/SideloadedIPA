# frozen_string_literal: true

source 'https://rubygems.org'

# Latest stable at last update (2026-06-06); Gemfile.lock pins exact versions for CI.
gem 'fastlane', '~> 2.235'
# Required at runtime by representable (google-apis-*); not always pulled transitively.
gem 'multi_json', '~> 1.21'
